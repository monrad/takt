# Review: sweep-the-open-issue-backlog-fix-the task 12 — rework

The core PR generation and stable-brief refactor are implemented, but the emitted command violates the task's explicit template contract, and the added malformed-record recovery can discard an assessment without triggering reassessment.

- **major** internal/brief/templates/run-push_pr.md:4 — The generated command does not match the required template: The task explicitly requires `--body-file {{.PRBodyPath}}` and a test rendering `--body-file /b/finish/pr.md`. The implementation instead adds PRBodyPathQuoted and emits `--body-file '{{.PRBodyPathQuoted}}'`, while changing the test fixture and expectation to match that unrequested API and output. Although shell-quoting the path is defensible, it violates the specified command contract and means the mandated rendering is never tested.
- **major** internal/cli/cmd_next.go:295 — The suggested recovery does not reassess deleted goal results: factsHint advises deleting malformed finish/goals.json so `takt next` can assess the goals again. Deleting the file leaves state.json's GoalsCheckedSHA intact; gatherFinishFacts can therefore still consider goals checked, proceed to push_pr, and BuildPR renders every goal as `not assessed` rather than redispatching the assessor. The hint should not recommend deletion unless the checked-goals state is also invalidated.
- **minor** internal/finish/pr.go:120 — Empty waiver reasons are treated as no waiver: The requirement says to render `waived (<reason>)` whenever rec.Waived contains the goal ID. This code tests whether the mapped reason is non-empty instead of testing key presence, so a readable record containing `"G1": ""` falls through to its verdict or `not assessed`. ReadGoals performs no validation that excludes such records, and the tests cover only a non-empty waiver reason.

_copilot / gpt-5.6-sol_

## Internal findings (confirmed)

- [lens:tests] major internal/cli/cmd_next.go:1037 — preparePushPR's own finish.ReadGoals error branch is never exercised by any test: The task requires that ANY error from `finish.ReadGoals(r.bdir)` inside preparePushPR (line 1037-1039) be returned. But `goalFacts` (internal/cli/finish_facts.go:186) already reads finish/goals.json unconditionally during fact-gathering, earlier in the same `next` call, and its failure is what actually surfaces (via factsHint) before decide ever produces the push_pr op. The test that appears to target this branch, TestPushPRBodyListsGoalVerdicts's third failure case (internal/cli/finish_test.go:668-680, corrupting finish/goals.json), explicitly acknowledges in its own comment that the failure comes from fact-gathering, not from this line ('It is the finish facts that decode the record on the way to this op, so they are what fails'). No test drives execution to a state where fact-gathering succeeds but this line's own ReadGoals call fails, so the defensive branch the task explicitly required has zero coverage.
- [lens:consistency] major internal/cli/cmd_next.go:1071 — prGoals double-names goals.md in its error, diverging from every other goals.Parse caller: prGoals wraps a goals.Parse failure with `fmt.Errorf("%s: %w", p, err)` where p is the full path (internal/cli/cmd_next.go:1066-1073). But every error goals.Parse itself returns already begins with the literal prefix "goals.md:" (internal/goals/goals.go:80, 84, 98, 104, 114), so the wrap produces a doubled, confusing message like "/…/docs/takt/demo/goals.md: goals.md: no goals under `## Goals`". Every other caller of goals.Parse in this package — cmd_goals.go:38-41, cmd_done.go:163-166, cmd_record.go:210-213, and cmd_next.go's own assessorBrief at line 876-878 — passes the error through unwrapped via err.Error(), relying on goals.Parse's built-in file-naming. prGoals is the only caller that adds a second, redundant name for the same file, and it is inconsistent with the neighboring os.ReadFile error two lines above it in the same function, which is correctly left unwrapped (os.ReadFile errors already carry the path via Go's own PathError formatting, exactly like goals.Parse's messages already carry "goals.md:").
- [lens:tests] major internal/decide/questions.go:399 — The rewritten pr option Description (title/body-file wording) has no test coverage: The pr option's Description was rewritten from the old `--fill` wording to the new `gh pr create --base <base> --title '<title>' --body-file <path>` text naming the op's pr_title/pr_body_path inputs (questions.go:399-403) — this is exactly the user-facing text a session reads to know how to open the PR, and the task explicitly required '--fill' be gone from the file. No test in the diff asserts on this Description's content. internal/decide/finish_test.go's branch_finish table test (around line 104-114) only checks the action type and step, never option text, so a regression here (stale flag name, wrong input key, reintroduced --fill) would not be caught by any test.
