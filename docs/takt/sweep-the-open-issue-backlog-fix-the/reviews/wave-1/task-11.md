# Review: sweep-the-open-issue-backlog-fix-the task 11 — approve

The change implements the requested event-based review counting and per-closed-attempt timing semantics correctly. Review findings are captured before carry-forward, persisted in close records and wave_closed events, gate/task totals derive from the specified event keys, timings pair and sort by wave/slice/attempt with legacy slice handling, and zero committed_at values are omitted. The added tests cover reworked attempts, uncommitted closes, JSON omission, legacy events, and CLI event persistence.


_copilot / gpt-5.6-sol_

## Internal findings (confirmed)

- [lens:docs] major internal/brief/templates/run-retro.md:1 — Retro instructions still say "dispatch→commit timings" after WaveTiming became dispatch→close: Task 11 changes WaveTiming from one entry per committed attempt to one entry per dispatched attempt that *closed*, with `closed_at` always present and `committed`/`committed_at` only when the attempt actually committed (internal/finish/retro.go's WaveTiming doc comment now reads "one per dispatched attempt that closed"). The retro-writing op's own instructions, run-retro.md line 1, still describe the inputs as "per-wave dispatch→commit timings" — unchanged except for the review-findings clause the task explicitly edited. A reworked attempt's timing entry has no `committed_at` at all, which the stale phrasing does not prepare the retro-writing agent for.
- [lens:tests] minor internal/cli/cmd_close_wave.go:163 — reviewFindingsOf's summation is only ever exercised with one reviewed task: reviewFindingsOf sums len(tr.Review.Findings) across every task in the close. The only test that reaches it, TestWaveClosedEventCarriesSliceAndReviewFindings (internal/cli/close_events_test.go), bumps a single task (task 3) with one finding, so the accumulation (n += ...) across two-or-more reviewed tasks in the same close is never proved. TestRetroInputsCountEveryReviewOnce (internal/finish/retro_test.go) tests the downstream retro math but constructs its wave_closed events with a hard-coded review_findings value rather than calling reviewFindingsOf, so it doesn't cover this function either. A silent regression here (e.g. assignment instead of accumulation) would produce quietly wrong review_findings counts in the retro with no failing test.
- [lens:consistency] minor internal/finish/retro.go:118 — retro.go re-declares gate.EvReviewed under a different local name: evGateReviewed = "gate_reviewed" duplicates the exported gate.EvReviewed (internal/gate/gate.go:47, also "gate_reviewed") even though retro.go already imports internal/gate (used elsewhere in the file, e.g. gate.FollowUp, gate.SourceApprove). Task 10's cmd_review.go uses gate.EvReviewed directly for the same event type; task 11 instead re-declares the same string under a new unexported name rather than reusing the existing constant it could import.
