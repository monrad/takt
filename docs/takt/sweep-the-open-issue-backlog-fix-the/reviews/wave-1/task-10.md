# Review: sweep-the-open-issue-backlog-fix-the task 10 — approve

The implementation matches the required persistence order, forced-receipt retirement, reason propagation, review-record hash/round format, legacy decoding, and commit-failure recovery. The supplied tests cover the requested behavior.

- **nit** internal/cli/cmd_review_test.go:67 — Legacy coverage was added as a new top-level test: The task says every new test belongs in cmd_review_failure_test.go and asks for the legacy-file scenario as an added case in the existing round-trip test. Keep this package-private coverage in cmd_review_test.go, but fold it into TestWriteResultJSONRoundTripsFindings rather than introducing a separate test function.

_copilot / gpt-5.6-sol_

## Internal findings (confirmed)

- [lens:docs] minor internal/cli/cmd_review.go:389 — priorFindingsForScopedPass's doc comment now contradicts the struct it sits above: The comment says "the findings file carries no hash and no pass identity, so pairing it with the receipt was convention, not a checkable invariant" (lines 388-390), but this same diff adds exactly that — Hash and Round — to reviews/<gate>.json via the new reviewRecord type documented two paragraphs above (lines 359-365: "tagged with the gate hash it reviewed and the round it was"). The stated justification for reading both the verdict and the blocking flag from one artifact is now factually wrong about the artifact's own shape, even though the conclusion (read from reviews/<gate>.json, not the receipt) may still be correct for other reasons.
- [lens:tests] minor internal/gate/gate.go:211 — RemoveReceipt's not-exist branch has no direct or indirect test coverage: RemoveReceipt (gate.go:211) is new and has two branches: delete an existing receipt, or treat fs.ErrNotExist as success. Every test that calls `takt review --force` (TestForcedReviewFailureLeavesNoStaleReceipt and TestFindingsFileCarriesTheGateHashAndRound in internal/cli/cmd_review_failure_test.go) does so only after a prior successful review already wrote a receipt, so the deletion always removes an existing file. No test — neither a direct unit test in internal/gate/gate_test.go nor a CLI test doing `review <gate> --force` as the very first review of a gate — exercises the fs.ErrNotExist-is-ok branch, even though that is a realistic first-use case for --force.
- [lens:consistency] minor internal/gate/gate.go:212 — RemoveReceipt uses fs.ErrNotExist where the rest of gate.go uses os.ErrNotExist: ReadReceipt (line 179) and artifactHash (line 125) both check errors.Is(err, os.ErrNotExist) for the identical sentinel. The new RemoveReceipt checks errors.Is(err, fs.ErrNotExist) instead, which required adding a new "io/fs" import solely for a spelling that os.ErrNotExist (== fs.ErrNotExist) already covers via the package's existing "os" import. Minor departure from the file's own established convention.
