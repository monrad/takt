# Review: sweep-the-open-issue-backlog-fix-the task 13 — rework

The exact LogID recording and lookup behavior is implemented, but one required leak assertion is weakened and the malformed-event test leaves the hash edge case untested.

- **major** internal/cli/close_internal_test.go:350 — Blind prompt test permits the forbidden lens name: The task explicitly requires asserting that the prompt does not contain "correctness", but the test instead requires exactly one occurrence and explains it away as rubric prose. This weakens the requested tripwire and allows the blind reviewer to see the forbidden term.
- **minor** internal/gate/gate_test.go:168 — Malformed hash handling is not exercised: Because both fields are malformed, the invalid gate can reject the event before its hash is inspected. The test would still pass if a non-string hash panicked whenever the gate was the valid string "spec"; add that case separately.

_copilot / gpt-5.6-sol_

## Internal findings (confirmed)

- [lens:tests] major internal/gate/gate_test.go:162 — TestRevisionEventMalformedDataDoesNotPanic does not actually test a malformed hash field: The test's doc comment claims 'a non-string gate and a non-string hash must fail the match, not panic', but revised() (internal/gate/gate.go, the EvRevisionAccepted path) filters on the 'gate' field first and `continue`s the loop when it doesn't match a string ('gate': []any{"spec"} fails the type assertion), before ever calling eventString(e, "hash") for that event. So the malformed hash value in the fixture (Data: map[string]any{"gate": []any{"spec"}, "hash": map[string]any{}}) is never actually parsed. This differs from its twin, TestOverrideEventMalformedDataDoesNotPanic (gate_test.go:134), whose target code (gate.go:238-239, `Compute`) reads both `gate` and `hash` unconditionally on every iteration before the combined boolean check short-circuits — so that twin does exercise the malformed-hash type assertion, but this new test does not.
- [lens:tests] major internal/gate/gate_test.go:187 — TestNilSeveritiesIsNotBlocking is a fake test — it cannot fail: The test asserts that gate.Compute reports Blocking == false for a Receipt with Severities: nil. The production line under test, `st.Blocking = r.Severities["blocking"] > 0` (internal/gate/gate.go:249), is a plain map key read; in Go, reading a key from a nil map is always safe and returns the zero value, never panics, regardless of any refactor that keeps this shape. The test therefore passes for any correct or incorrect implementation that keeps the same map-lookup pattern, and could only fail if the code were rewritten to something unrelated (e.g. an explicit nil check that panics) — it adds no real regression protection despite the doc comment's 'pins that ... computes Blocking == false rather than panicking on a nil map read' framing.
