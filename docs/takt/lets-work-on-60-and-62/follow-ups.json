{
  "items": [
    {
      "gate": "plan",
      "severity": "major",
      "title": "Task 1 tests the fake seam, not the real runCLI fallback",
      "detail": "Task 1’s cross-package test drives fakeReviewer.Review, which newly calls resolveTimeout itself. It can pass even if runCLI—the production path named by A1—does not call resolveTimeout or applies a different unset-timeout fallback. The grep checks only that resolveTimeout exists. Add verification that exercises or otherwise directly pins runCLI’s use of the shared resolver; otherwise G1’s no-drift evidence is indirect and defeatable.",
      "source": "override",
      "ts": "2026-08-29T11:27:53.51200711Z"
    },
    {
      "gate": "plan",
      "severity": "minor",
      "title": "Task 1 disagrees about the explicit-timeout cancellation row",
      "detail": "The plan.md Task 1 section requires a second row with a short explicit Timeout and longer fake sleep, but plan.index.json Task 1 specifies only the unset-timeout fallback test and its verify selector does not clearly require the cancellation row. Either include the row explicitly in the executable task or remove it from plan.md.",
      "source": "override",
      "ts": "2026-08-29T11:27:53.512008015Z"
    },
    {
      "gate": "plan",
      "severity": "minor",
      "title": "Boundary tests weaken a representable strict-containment case",
      "detail": "Task 2 classifies MaxDuration-SessionMargin and MaxDuration-Grace as saturation points where only non-strict bounds are asserted. At exactly those inputs, adding the margin produces representable MaxDuration and is still strictly greater than the input. Assert the strict relation at equality, reserving the non-strict-only rule for inputs above the threshold.",
      "source": "override",
      "ts": "2026-08-29T11:27:53.51200805Z"
    },
    {
      "severity": "blocking",
      "file": "commands/takt.md",
      "line": 45,
      "wave": 0,
      "title": "Prompt invariant \"never push except push_pr\" is now false: archived pr cleanup pushes too",
      "detail": "This wave's archive.go change (prCleanup, archive.go:179-198) makes an `archived` stop's `cleanup` list carry a literal `git push origin \u003cbranch\u003e` / `git push -u origin \u003cbranch\u003e` command for the pr disposition — a second place a takt op instructs a push, not covered by this line's sole named exception `(push_pr)`. The op-table's `stop` bullet (commands/takt.md:33) already generically tells the agent to run confirmed cleanup commands, so this line now contradicts it inside the same file: an LLM strictly honouring the Invariants section's 'never push except push_pr' could refuse the very cleanup push task 7 exists to hand back. The identical stale sentence is duplicated in hosts/copilot/skills/takt/SKILL.md:46 (same text, not part of the crossHostInvariants test set, so it won't be caught by TestPromptInvariantsReadTheSameOnEveryHost either). Neither file is mentioned anywhere in this run's own docs/takt/lets-work-on-60-and-62/plan.md task 8, which only targets README.md and the design spec — so this gap is not already tracked to be fixed by a later task in this run.",
      "source": "internal",
      "ts": "2026-08-29T12:02:45.045201947Z"
    },
    {
      "severity": "minor",
      "file": "internal/backend/fake.go",
      "line": 45,
      "wave": 0,
      "task": 1,
      "title": "recordReviewDeadline's failure path is never exercised",
      "detail": "The new block `if p := f.getenv(\"TAKT_FAKE_REVIEW_TIMEOUT_FILE\"); p != \"\" { if err := recordReviewDeadline(ctx, p); err != nil { return errorResult(...) } }` (fake.go:45-49) has no test that makes the write fail (e.g. a path in a nonexistent directory), so the errorResult branch this new code adds is untested. TestBackendFallbackMatchesTheShippedDefault only exercises the success path via a valid t.TempDir() file.",
      "source": "internal",
      "ts": "2026-08-29T12:08:06.656143429Z"
    },
    {
      "severity": "nit",
      "file": "internal/cli/brief_stable_test.go",
      "line": 188,
      "wave": 0,
      "task": 6,
      "title": "Added assertion re-invokes commitBundle rather than checking preparePushPR's own call",
      "detail": "Lines 188-191 call `commitBundle(context.Background(), r.ws, r.bdir, r.slug, \"pr body\")` a second, separate time to assert `committed==false`. Since `r.ws` is the same zero-value workspace (Dir.InRepo false) that preparePushPR's own internal commitBundle call at line 171 already ran through without error, this second call is a fresh, independent invocation rather than an observation of what preparePushPR actually did — it documents commitBundle's no-op behaviour in isolation more than it verifies the integration point, though it does satisfy the brief's explicit request for 'one assertion making that explicit'.",
      "source": "internal",
      "ts": "2026-08-29T12:08:06.657020915Z"
    },
    {
      "severity": "minor",
      "file": "internal/cli/cmd_next.go",
      "line": 1066,
      "wave": 0,
      "task": 6,
      "title": "preparePushPR's new commitBundle error path is untested",
      "detail": "The new `if _, _, err = commitBundle(ctx, r.ws, r.bdir, r.slug, \"pr body\"); err != nil { return err }` propagates a commit failure exactly like the pre-existing read/write failures preparePushPR already returns, but no test forces commitBundle to fail here (e.g. a broken repo/lock) to confirm the error surfaces through preparePushPR and ultimately fails `run` the same way assertPreparePushPRRefuses covers for the spec/goals reads. The codebase already has a pattern for simulating git-commit failures (internal/cli/archive_test.go's TestArchiveCommitIsRetriedAfterASoftReset) that could have been reused here.",
      "source": "internal",
      "ts": "2026-08-29T12:08:06.657021017Z"
    },
    {
      "severity": "minor",
      "file": "internal/cli/archive_test.go",
      "line": 696,
      "wave": 0,
      "task": 7,
      "title": "Cleanup-empty checkpoints skip op/reason assertions",
      "detail": "In TestArchivedPROffersThePushUntilItIsDone (also at line 710), the checks `len(got) != 0` / `len(again) != 0` read `o[\"cleanup\"]` without first asserting `o[\"op\"] == \"stop\"` and `o[\"reason\"] == stopArchived` (as the earlier and later checkpoints in the same test do, e.g. lines 673-674, 683-684, 722-723). cleanupOf silently returns an empty slice when the \"cleanup\" key is absent or of the wrong shape, so if `next` regressed to returning a different op entirely at these points, the 'nothing offered' assertions would pass anyway, masking the regression in exactly the rows this test exists to pin (G7's zero-cleanup cases).",
      "source": "internal",
      "ts": "2026-08-29T12:42:47.857600296Z"
    },
    {
      "severity": "minor",
      "file": "internal/cli/cmd_close_wave.go",
      "line": 121,
      "wave": 1,
      "task": 3,
      "title": "closeBudget's nil-ActiveWave guard is untested and unreachable",
      "detail": "closeBudget's `if st.ActiveWave == nil { return b }` branch (lines 121-123) has no test in close_budget_test.go. It's also unreachable in production: closeBudget is only called from closeWaveBudgeted, which cmdCloseWave only reaches after already checking tgt.st.ActiveWave != nil. Low risk since it's dead code on the real call path, but as written it's a new branch a regression in could go uncaught.",
      "source": "internal",
      "ts": "2026-08-29T13:09:01.537915978Z"
    },
    {
      "severity": "minor",
      "file": "internal/cli/cmd_close_wave.go",
      "line": 126,
      "wave": 1,
      "task": 3,
      "title": "The 'counted set' predicate is duplicated inline instead of shared",
      "detail": "closeBudget's task filter `t.Wave != st.ActiveWave.N || t.Status != bundle.StatusPending` (line 126) is a verbatim copy of resolveTaskResults' filter `t.Wave != aw.N || t.Status != bundle.StatusPending` (line 280). Both doc comments explicitly assert the two sets must stay identical (closeBudget's comment: 'the set resolveTaskResults grades'), and task-4's cross-check integration test that would guard this invariant doesn't exist yet in this wave. With the predicate spelled out twice rather than factored into one helper (e.g. a shared `pendingWaveTasks(st)` iterator), a future edit to one loop's condition can silently desync the deadline's counted work from the work actually graded, with nothing but human review catching it until the task-4 test lands.",
      "source": "internal",
      "ts": "2026-08-29T13:09:01.537916166Z"
    },
    {
      "severity": "major",
      "file": "docs/superpowers/specs/2026-08-24-takt-design.md",
      "line": 462,
      "wave": 2,
      "title": "Design spec still shows the deleted fixed close-wave timeout as the canonical exec example",
      "detail": "The `exec` example for `takt close-wave` still reads `\"timeout_s\": 1800` — the exact value of the now-deleted `closeTimeoutS` constant. This diff removes that constant and makes the close-wave exec op's timeout_s a computed value (`sessionSeconds(deadline.Close(deadline.Budget{...}))`, spec A2.2), which generally will not be 1800 (it scales with VerifyCommands/ReviewTasks/BackendTimeout/VerifyTimeout and is strictly greater than deadline.Close's own result). This is the master design reference under docs/superpowers/specs/ that the rubric names explicitly, and it now documents the pre-A2.2 fixed-timeout behaviour this wave's diff replaces. The spec/plan docs that guided this task (docs/takt/lets-work-on-60-and-62/plan.md and spec.md) do not list this file among the ones to update, so nothing else in the run addresses it.",
      "source": "internal",
      "ts": "2026-08-30T13:30:28.562940404Z"
    },
    {
      "severity": "major",
      "file": "docs/superpowers/specs/2026-08-24-takt-design.md",
      "line": 337,
      "wave": 4,
      "task": 8,
      "title": "§4.7 commit-message catalog omits the new \"pr body\" commit",
      "detail": "The \"Commits.\" bullet enumerates every message takt commits with (`wave \u003cn\u003e — tasks…`, `plan → execute`, `archive`) but not the `takt(\u003cslug\u003e): pr body` commit that `preparePushPR` (internal/cli/cmd_next.go) now makes when it writes `finish/pr.md`, before the session's `push_pr` runs `git push`. This is a branch-wide behavior change (run's own spec.md: \"next commits finish/pr.md in the commit the push carries\", goal G6, tested by TestPushPRLeavesTheBodyInHead) that this final docs wave did not fold into the invariant list it lives next to.",
      "source": "internal",
      "ts": "2026-08-30T14:20:04.977719798Z"
    },
    {
      "severity": "minor",
      "file": "docs/superpowers/specs/2026-08-24-takt-design.md",
      "line": 487,
      "wave": 4,
      "task": 8,
      "title": "§5.2's \"exactly when\" wording for pr cleanup is looser than §7.5 step 5's own case list",
      "detail": "§5.2 (lines 486-488) says a `pr` archive carries the push as `cleanup` \"exactly when the remote-tracking ref is missing commits.\" §7.5 step 5 (lines 892-897), added in the same diff and describing the same `prCleanup` predicate, adds a case §5.2 omits: \"a failed git read still offers the push\" — which matches `internal/cli/archive.go`'s `prCleanup` (both `CommitExists` and `IsAncestor` error paths return the push regardless of whether the ref is actually missing anything). So on a read failure the push can be offered even when the ref is not actually missing commits, which the §5.2 sentence's \"exactly when\" does not account for. §7.5's fuller statement is accurate; §5.2's summary overclaims precision. Low impact since §5.2 cross-references §7.5 step 5 for the mechanism, but the two sentences describe the same rule slightly differently within the same document.",
      "source": "internal",
      "ts": "2026-08-30T14:20:04.97772009Z"
    },
    {
      "severity": "minor",
      "file": "docs/superpowers/specs/2026-08-24-takt-design.md",
      "line": 814,
      "wave": 4,
      "task": 8,
      "title": "review_error gate's dynamic retry text (backends.\u003cname\u003e.timeout) is undocumented",
      "detail": "internal/decide/questions.go's questionReviewError now names the specific `backends.\u003cname\u003e.timeout` key (and its current deadline) in the retry option's description when the review errored, falling back to the literal placeholder key when no backend can be named — a branch change (run spec G5). Line 814 still describes the gate only as \"(retry / skip this task's review with reason / stop)\" with no mention of this. This matches the doc's existing terseness for other gates' option text, so it's a minor gap rather than a clear regression.",
      "source": "internal",
      "ts": "2026-08-30T14:20:04.977720291Z"
    },
    {
      "severity": "major",
      "file": "docs/superpowers/specs/2026-08-24-takt-design.md",
      "line": 869,
      "wave": 4,
      "task": 8,
      "title": "§7.5 step 4's `pr` disposition prose doesn't mention finish/pr.md is committed before the push",
      "detail": "The paragraph describing `pr: run push_pr` says the body file path (`inputs.pr_body_path`, naming `finish/pr.md`) is handed to the session for `gh pr create --body-file`, but never says takt commits that file into the branch first (subject `pr body`) so the pushed branch already carries the PR body it was created from. A reader of this section would still believe the body is only written to disk, not committed — the exact behavior issue #62's first half changed, per the run's own spec.md and internal/cli/cmd_next.go's preparePushPR doc comment (\"Committing here... is what puts finish/pr.md in the branch before the session pushes it\").",
      "source": "internal",
      "ts": "2026-08-30T14:20:04.97772041Z"
    }
  ]
}
