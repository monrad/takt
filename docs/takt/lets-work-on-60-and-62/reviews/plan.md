# Review: plan — rework

The decomposition is broadly sound and covers the requested behavior, but several load-bearing facts and git branches are only tested in isolation. The specified verification could pass while deadline containment, backend filtering, or archived-PR cleanup is wired incorrectly.

- **major** plan.md:0 — Task 4 does not verify CLI-to-decide deadline facts: Task 3 tests closeBudget and Task 4 tests internal/decide using manually populated Facts, but no planned test exercises gatherFacts, gatherWaveFacts, or gatherFinishFacts and compares their values with the binary-side budget. VerifyCommands, ReviewTasks, or finish command counts could remain zero or use the wrong task set while every listed verify command passes, so A2.2/G4's same-work containment is not proved.
- **major** plan.md:0 — Task 5 does not test reviewer-chain fact gathering: TestReviewErrorNamesTheBackendTimeouts constructs ReviewerBackends directly, while Task 1 tests the accessor separately. Nothing verifies that gatherFacts iterates ws.Cfg.Backends.Reviewer in preference order, preserves configured durations, and skips fake and unknown names. A broken or empty integration would still satisfy all listed checks. Add a package-cli test that gathers facts from a mixed configured chain and asserts the resulting slice and rendered gate.
- **major** plan.md:0 — Task 7 misses two explicitly required git branches: The planned flow covers a missing tracking ref, equality, and a strictly-ahead branch; the cancelled-context test fails CommitExists before IsAncestor runs. It does not exercise a diverged history or an IsAncestor error. Because the verify commands do not even require an IsAncestor call, an ahead-only implementation or an implementation that propagates the second read error could pass despite contradicting B2. Add explicit divergent-history and second-read-failure cases.
- **minor** plan.md:0 — Task 1 does not explicitly pin unknown-backend lookup behavior: The listed accessor cases cover fake and several known-backend chains but do not explicitly call Timeout with an unknown name, even though A3 requires both fake and unknown names to report no real config key. This should be a direct table row rather than inferred from the fake case.

_copilot / gpt-5.6-sol_
