# Review: plan — rework

The decomposition is broadly sound and covers the spec, but execution would start from contradictory API instructions and an invalid first-render data shape. These must be corrected before implementation.

- **blocking** :0 — Tasks 3 and 4 contradict Task 2's GateReview API: Task 2 and the spec define deadline.GateReview(backend time.Duration) with Grace owned internally, while plan.index tasks 3 and 4 instruct callers to pass deadline.Grace as a second argument. Task 2 also requests monotonicity testing over a nonexistent grace input. The corresponding plan.md sections use the correct one-argument form. Normalize tasks 2–4 to the one-argument API and test monotonicity only in the backend-duration argument; otherwise following the indexed tasks produces code that cannot compile.
- **major** :0 — Task 5's specified context representation cannot render correctly before JSON round-trip: Task 5 says decideActiveWave should store backends as []map[string]any but questionReviewError should decode it as []any containing map[string]any. The ask path calls Question immediately with the original context, before persistence converts []map[string]any into []any, so the initial gate would fall back to backends.<name>.timeout instead of rendering the configured entries. Require the renderer to accept both []map[string]any and []any, or construct []any initially; retain a test of the direct decide.Decide path as well as the persisted round-trip.
- **minor** :0 — Task 1 records a resolved duration without proving the fake honors it: Task 1's new test reads TAKT_FAKE_REVIEW_TIMEOUT_FILE, which can pass if fakeReviewer writes resolveTimeout(req.Timeout) but continues using the original context. That does not prove the description's claim that the recorded deadline is actually applied around fakeDelay and the remaining review. Add a short explicit request timeout with a longer TAKT_FAKE_REVIEW_SLEEP and assert the fake returns the timeout error result, while keeping the unset-timeout recording assertion for mirrored-default equality.

_copilot / gpt-5.6-sol_
