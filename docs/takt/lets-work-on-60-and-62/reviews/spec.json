{
  "verdict": "rework",
  "summary": "The overall approach is plausible, but planning must wait: the timeout envelope is mathematically insufficient, the outer and inner close deadlines contradict the stated strict-containment rule, and one success check cannot pass as written. Backend mapping and several required behaviors also lack testable decisions.",
  "findings": [
    {
      "severity": "blocking",
      "file": "spec.md",
      "line": 42,
      "title": "The close-wave envelope undercounts verification time",
      "detail": "The formula budgets one `defaultVerifyTimeout`, but the code runs every verify command serially for every done task before review (`taskOutcome` calls `wave.RunVerify`, whose loop executes each command sequentially). Plans impose no one-command limit. One task with four commands can consume 40m before up to 30m of review, so the proposed 60m cap can expire. Define a bound based on command/task multiplicity, derive the deadline dynamically, or change the execution model."
    },
    {
      "severity": "blocking",
      "file": "spec.md",
      "line": 53,
      "title": "Equal outer and inner close deadlines violate strict containment",
      "detail": "The spec says wrapping deadlines must strictly exceed their worst case, but permits `closeTimeoutS \u003e= closeWaveTimeout` and chooses 60m for both. Startup, cancellation, result serialization, and process-exit overhead mean the session deadline can fire before `takt close-wave` reports its own timeout result. Require and budget a positive outer margin."
    },
    {
      "severity": "blocking",
      "file": "goals.md",
      "line": 14,
      "title": "The documented grep success check is impossible",
      "detail": "`grep '\"timeout\": \"5m\"' README.md docs/` also searches this run's `docs/takt/lets-work-on-60-and-62/spec.md`, whose line 80 intentionally contains that exact old-value string. The command will still return a match after the target examples are updated. Restrict the check to the two intended documents or use a context-specific assertion."
    },
    {
      "severity": "major",
      "file": "spec.md",
      "line": 149,
      "title": "The proposed envelope test cannot observe all three constants",
      "detail": "The test is assigned to `internal/decide`, but `closeWaveTimeout` is an unexported constant in `internal/cli`; the other two constants are also private. The spec must choose a shared/exported deadline representation or split the assertions across packages. As written, one decide test cannot fail when any of the three constants changes."
    },
    {
      "severity": "major",
      "file": "spec.md",
      "line": 67,
      "title": "Deadline rendering is undefined for valid non-shipped reviewer names",
      "detail": "`backends.reviewer` is not validated to only `copilot` and `claude`; the registry also contains `fake`, and unknown names are tolerated until selection. `config.Backends` has timeout fields only for copilot and claude, so `backends.fake.timeout` or an unknown backend has no current deadline or real configuration key. Specify whether such entries are omitted, rendered with the literal placeholder, rejected by config validation, or represented through a generalized backend map."
    },
    {
      "severity": "major",
      "file": "goals.md",
      "line": 9,
      "title": "The unset backend fallback is not actually tested",
      "detail": "G1 requires the unset-`Timeout` fallback to agree with the shipped default, but its evidence only asserts config defaults and says backend tests pass. The testing section likewise specifies no assertion of `internal/backend/run.go`'s fallback. Add a direct cross-package equality test or expose a single shared default so drift is structurally impossible."
    },
    {
      "severity": "major",
      "file": "goals.md",
      "line": 13,
      "title": "Goals omit the specified git-read error behavior",
      "detail": "B2 explicitly requires git read errors to return `git push origin \u003cbranch\u003e` without failing the archived stop, but G5 and the testing section cover only ahead, fully pushed, and missing-ref states. Add an injected-error case and corresponding success criterion so goals.md matches the spec."
    },
    {
      "severity": "minor",
      "file": "spec.md",
      "line": 122,
      "title": "Not-an-ancestor does not mean strictly ahead",
      "detail": "`IsAncestor(\u003cbranch\u003e, origin/\u003cbranch\u003e) == false` also covers diverged histories, not only a branch that is ahead. The cleanup may still be appropriate because local commits are absent remotely, but the requirement and documentation should say “contains local commits not present in the remote-tracking ref” or explicitly define divergence behavior."
    }
  ],
  "provider": "copilot",
  "model": "gpt-5.6-sol",
  "hash": "sha256:c50115da2a3835db85c81ffaf197b01c8329b654071f7a8cb065e18417ae1b40",
  "round": 1
}
