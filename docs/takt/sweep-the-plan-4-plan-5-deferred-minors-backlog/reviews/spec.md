# Review: spec — rework

Two prior findings remain unresolved because revised text retains contradictory or incorrect statements.

- **blocking** spec.md:0 — `--force` would log non-takeovers: The ruling and T3 correctly require a stolen/forced outcome, but the Assumptions table still says to log whenever `r.force` is set. That contradiction could still prescribe false events for free or self-held locks.
- **minor** goals.md:5 — Anchor miscounts fixes and rulings: The anchor still describes fourteen fixes plus two rulings instead of eleven code/doc fixes, two rulings, and one already-fixed closure. G12 explains the arithmetic elsewhere but does not correct the anchor.

_copilot / gpt-5.6-sol_
