# Review: finish-phase-fixes-for-the-features-that task 3 — approve

The implementation matches the specified generator contract, preserves and anchors the reworded invariant across hosts, enforces ordered byte-exact substitutions with multiplicity errors, integrates strict skill generation into hostgen, and adds the required parity, stale-file, regeneration, and missing-input coverage. No actionable issues found.


_copilot / gpt-5.6-sol_

## Internal findings (confirmed)

- [lens:tests] minor internal/hosts/skill.go:153 — substitutionLabel's truncation branch is never exercised: substitutionLabel truncates a from region to labelMax (56) runes at line 153-154, but every case in TestRenderCopilotSkillErrorsWhenARegionDoesNotMatchItsCount (internal/hosts/skill_test.go) breaks the H1 substitution, whose from text ('# /takt — the op loop', 22 runes) never approaches 56. The frontmatter substitution's from field is far longer than 56 runes, so the truncation path this function exists for is never hit by a test.
- [lens:consistency] major internal/tools/hostgen/main.go:117 — generateSkill duplicates the agent loop's compare/check/write logic instead of sharing a helper: run()'s per-agent loop (lines 66-78: read dst, bytes.Equal skip, `check` → print "stale:" + count, else write() + print "wrote") is the same four-branch shape newly written again in generateSkill (lines 117-127) for the skill file. Both were touched/added by this wave's task-3. A small `writeOrReport(dst, out, check, stdout, stderr) (stale int, err error)` would serve both call sites; instead the diff reimplements the pattern rather than factoring it, which is exactly the 'duplicated helper' this lens looks for — and it is the same file, so a future edit to one copy (e.g. the stale-message wording) can silently drift from the other.
- [lens:consistency] nit internal/tools/hostgen/main.go:136 — manifestVersion names a function here but a struct/type for the same concept elsewhere in the codebase: internal/tools/hostgen/main.go:136 introduces `func manifestVersion(path string) (string, error)`, and internal/prompt/copilot_test.go:100 (this same wave) introduces a same-named test helper function. internal/cli/cmd_version.go:88 already has `type manifestVersion struct { Version string }` for the identical concept (the plugin manifest's version field). Different packages so it compiles, but the same identifier names a type in one place and a function in two others for what the brief itself treats as one recurring concept (spec §2.3's handshake version).
- [lens:tests] minor internal/tools/hostgen/main.go:144 — manifestVersion's JSON-unmarshal and empty-version error returns are untested: manifestVersion (main.go:136-151) has three error returns: file-read failure, json.Unmarshal failure (line 144-146), and an empty "version" field (line 147-149). Only the file-read failure is exercised, via TestRunRefusesARootMissingASkillSource's 'no manifest' case (main_test.go:177), which removes the file rather than corrupting or emptying its version. The other two returns are never triggered by any test.
- [lens:consistency] minor internal/tools/hostgen/main.go:148 — Missing-version-field error reads differently from the CLI's own phrasing for the same condition: hostgen's manifestVersion renders a missing/empty "version" field as `fmt.Errorf("%s: no \"version\" field", path)` (internal/tools/hostgen/main.go:148), while internal/cli/cmd_version.go:139 renders the identical condition — an empty version in the same .claude-plugin/plugin.json — as `"plugin manifest " + path + " has no version field"`. Same fact, two different sentence shapes and quoting conventions for the one JSON field this wave's task-3 cares about (§2.3's version handshake).
- [lens:tests] major internal/tools/hostgen/main_test.go:126 — generateSkill's RenderCopilotSkill error path (a render error is exitFailure) is never exercised: internal/tools/hostgen/main.go:112-115 returns the error from hosts.RenderCopilotSkill straight out of generateSkill, and run() (main.go:85-89) turns it into exitFailure — spec §2.3 explicitly names this half of the contract ('a render error is exitFailure'). Neither TestRunRegeneratesAHandEditedSkill nor TestRunRefusesARootMissingASkillSource (nor any other test in main_test.go) ever seeds a root whose commands/takt.md fails a substitution's declared count, so this branch of generateSkill/run is untested at the hostgen level — it is only exercised indirectly through hosts.RenderCopilotSkill's own unit tests in internal/hosts/skill_test.go, never through run()'s wiring of that error into exitFailure and its stderr message.
